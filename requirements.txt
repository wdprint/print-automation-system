# PDF 인쇄 의뢰서 자동화 시스템 - 필수 패키지
# Python 3.8 이상 필요

# 핵심 의존성
PyMuPDF==1.23.26        # PDF 처리 (fitz)
Pillow==10.2.0          # 이미지 처리
tkinterdnd2==0.3.0      # 드래그 앤 드롭 기능
numpy==1.24.4           # 수학 연산 (향상된 버전용)

# 빌드 도구 (exe 변환용)
pyinstaller==6.3.0      # exe 파일 생성

# 선택적 의존성 (개발용)
# pytest==7.4.4         # 테스트용
# black==23.12.1        # 코드 포맷팅